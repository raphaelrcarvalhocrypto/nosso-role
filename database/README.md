# Database

Pasta reservada para scripts e documentacao de banco de dados.

O app atual usa Supabase (Postgres + RLS + Storage), portanto os scripts SQL em `database/sql` sao parte ativa da stack e devem ser tratados como migrations versionadas.
